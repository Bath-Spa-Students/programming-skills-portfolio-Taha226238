numbers1 = [1, 2, 3, ...]  # Assuming numbers1 has 100 elements
numbers2 = [2]  # Initialize an empty list

for number in numbers1:
    numbers2.append(number)

# Now, numbers2 contains the same values as numbers1
